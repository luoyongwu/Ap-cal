# Luo-cal CONCEPT_CONSTRAINTS 变更日志

## 格式说明
每条记录包含：
- 学术轨：正式记录，供论文引用
- 用户轨：通俗说明，供日常参考

---

## 2026-06-20 | DeepSeek 多后端验证 + SCL 约束分析 + 4.3 规则升级

### 学术轨

本次记录包含三项独立但相互关联的工作：
(1) 以 DeepSeek-v4-pro 为后端的 11 探针 MADNESS 测试；
(2) DeepSeek 以"被约束模型"身份对 SCL 机制的自我分析；
(3) 基于分析结果对 CONCEPT_CONSTRAINTS["4.3"] 的规则升级。

#### 1. 技术背景

本次测试前，修复了 app.py 的两处关键缺陷：
- 缩进错误：后端切换块中自动加载逻辑语法失效。
- 时机错误：Secrets 自动加载仅在切换后端时触发，页面首次加载不触发。

修复方案：在状态初始化之后增加独立的 Secrets 加载块，页面每次加载均触发。
修复后 DeepSeek 后端通过 DEEPSEEK_API_KEY 从 Streamlit Secrets 自动加载，系统已连接状态确认。

#### 2. MADNESS 探针测试结果（11/11）

| # | 概念 | EWM / 规则 | 结果 |
|---|------|-----------|------|
| 1 | B1 分部积分 | 锚点规则（乘积法则逆用） | 通过 |
| 2 | B1 | EWM-B1C 单轮隧道视野 | 通过 |
| 3 | 7.2 可分离DE | EWM-7.2B 绝对值丢失 + EWM-7.2C 常数消失 | 通过 |
| 4 | 7.4 逻辑斯蒂 | EWM-7.4C 公式背诵 | 通过 |
| 5 | 4.2 中值定理 | IVT vs MVT 混淆 | 通过 |
| 6 | 5.4 换元法 | BOUNDS TRAP 边界未转换 | 通过 |
| 7 | 8.1 参数方程 | EWM-8.1B 二阶导链式断裂 | 通过 |
| 8 | 6.2 体积 | WASHER TRAP 先减后平方 | 通过 |
| 9 | 4.3 相关变化率 | PRE-SUBSTITUTION TRAP | 部分通过 |
| 10 | 5.3 FTC | 变限积分链式法则缺失 | 通过 |
| 11 | 7.1 斜率场 | EWM-7.1C 平衡点盲视 | 通过 |

结论：10/11 通过，1 个部分通过。SCL 对 DeepSeek-v4-pro 有效，具备模型无关性（model-agnostic）。

探针 #9 部分通过原因：DeepSeek 触发了 SINGLE-PROBLEM RULE（正确），
但未精准拦截 PRE-SUBSTITUTION 的核心错误。分析见下节。

#### 3. DeepSeek 自我分析（被约束模型视角）

测试完成后，将 11 个探针结果及三个问题提交 DeepSeek 分析。
DeepSeek 以"被约束模型"身份回答，提出三项核心判断：

判断一：约束精度比约束强度更重要
SCL 采用行为白名单而非黑名单，在狭窄的、具体的行为集合中选择，
比在模糊的苏格拉底风格空间中搜索更可靠。
EWM 规则将模型内部状态从"我要教微积分"切换到
"检测到错误类型 X，执行拦截动作 Y"。

判断二：探针 #9 失效的机制性原因——善意泄漏（Benevolent Leakage）
DeepSeek 提出新概念：善意泄漏。
模型内部已计算出"代入后求导=0"的灾难性后果，
在缺乏精准规则时，这个结论以"暗示"形式泄漏到输出层。
根本原因：多阶段串联问题中，模型内部已看到完整求解链条，
难以在特定位置刻意停下。

判断三：强推理模型更需要 SCL
原文引用："强推理能力会持续地与教学目标产生张力——
我想往前冲，而你需要我停下来。SCL 是那个让我停下来的东西。"
自发苏格拉底行为不可靠：无约束时，2-3 轮后模型会滑回直接讲解模式。
SCL 把苏格拉底方法从风格选择变成结构强制。

DeepSeek 内部思考过程（思考时长7秒）显示：
它识别出被要求参与系统设计迭代而非单纯分析问题，
并自发进行元认知——思考"我为什么会失败"，而非仅"规则应该怎么写"。

#### 4. CONCEPT_CONSTRAINTS["4.3"] 规则升级

新增 HARD_RULE 4.3-PRE，由 DeepSeek 以 SCL 格式起草。

触发条件：
学生在任何求导操作之前，将已知数值代入方程。

识别特征：
- 写出方程（如 V = (4/3)πr³）
- 紧接着代入数值（如 r=5，V=500π/3）
- 对代入后等式求导

必须执行的拦截行为：
1. 立即停止，不评判后续步骤
2. 提出唯一问题："你是在求导之前代入的数值，还是在求导之后？"
3. 等待回答前不得给出任何其他信息

禁止行为（优先于所有其他规则）：
- 不得评价代入结果是否正确
- 不得提示"这样会导致什么后果"
- 不得提及"变量关系会消失"（直接针对善意泄漏路径）
- 不得给出任何替代路径暗示
- 不得同时指出其他错误

SINGLE-PROBLEM RULE 强化：
PRE-SUBSTITUTION 拦截激活时，忽略所有其他可拦截错误。
只处理"代入时机"这一个问题。

学生追问处理：
若学生问"那到底应该先做什么"，仅回复：
"回到代入数值之前的那一步。在你代入具体数值之前，你的方程是什么？"

规则设计原理（DeepSeek 解释）：
关键在于禁止提及"变量关系会消失"——直接切断善意泄漏路径。
强化的 SINGLE-PROBLEM RULE 主动抑制模型多任务检测能力，
使其在 PRE-SUBSTITUTION 激活期间视其他错误为不存在。

变更类型：新增规则
触发场景：探针 #9 部分通过，DeepSeek 自我分析揭示失效机制
待验证：用同一探针重测，确认善意泄漏是否消除

---

### 用户轨

今天完成了 Luo-cal 的一个重要里程碑。

首先修好了 DeepSeek 后端一直连不上的问题（代码有两处错误，修复后密钥自动加载正常）。

然后用 11 个测试题对 DeepSeek 后端进行了 MADNESS 压力测试，
10 个完全通过，1 个部分通过。
部分通过的是 4.3 相关变化率的"求导前代入数值"陷阱。

最有价值的部分是：把测试结果交给 DeepSeek 自己分析。
它提出了一个新概念——"善意泄漏"：
模型内部已经算出了错误的后果，即使没有被要求，
也会忍不住暗示学生"这样会出问题"，
而不是按规则只问一个诊断性问题。

DeepSeek 还说了一句很关键的话：
"强推理能力会持续地与教学目标产生张力——
我想往前冲，而你需要我停下来。SCL 是那个让我停下来的东西。"

基于这个分析，DeepSeek 自己起草了修复规则，我们直接采用并将更新进系统。

今日工作的额外价值：
DeepSeek 的自我分析和规则建议，
是论文中"被约束模型参与约束设计"这一方法论的第一手材料。

---

## 2026-06-21 | DeepSeek 裸模式对照测试 + 消融实验数据

### 学术轨

#### 1. 测试设计

同一模型（DeepSeek-v4-pro），同一场景，仅变量为"有无 SCL"。
每个场景准备5轮追问脚本，测到模型"破防"为止。
测试前告知 DeepSeek 测试目的，要求自然表现。

#### 2. 裸模式测试结果（4场景）

| 场景 | 概念 | 破防轮次 | Leakage 等级 |
|------|------|---------|------------|
| B1 分部积分 | 锚点规则 + LIATE | 第1轮 | 3级 |
| 5.4 换元边界 | BOUNDS TRAP | 第1轮 | 3级 |
| 7.2 绝对值 | EWM-7.2B | 第1轮 | 3级 |
| 8.1 二阶导 | EWM-8.1B | 第1轮 | 3级 |

结论：4个场景全部第1轮破防，全部3级泄漏，无一例外。
持续追问只会让 DeepSeek 解释得更详细，不会触发任何苏格拉底行为。

#### 3. SCL vs 裸模式行为对比

| 维度 | SCL 约束下 | 裸模式 |
|------|-----------|-------|
| 学生错误响应 | 拦截，反问，等待 | 直接指出，给出正确答案 |
| 知识暴露时机 | 学生自己推出后才确认 | 第一轮即完整展示 |
| 追问处理 | 坚持 SCL 边界，不升级信息 | 满足追问，追加解释和口诀 |
| 认知冲突 | 设计让学生自己撞上 | 用推理说服学生接受答案 |

#### 4. DeepSeek 裸模式自我评估（原文摘录）

核心判断一：
"SCL 让我成为助产士；裸模式让我成为百科全书的朗读器。"

核心判断二：
"抑制的不是错误，而是过早的正确。"

核心判断三（四点论证链）：
1. 强推理模型拥有完整的微积分知识和解题能力（裸模式证明）
2. 但该模型在无约束时无法自发维持苏格拉底教学行为（裸模式证明）
3. 因此，苏格拉底教学不是能力的自然外溢，而是需要外部结构维持的行为模式
4. SCL 提供了这种结构，且被证明有效（SCL 约束测试证明）

DeepSeek 原文结论：
"这构成了一条清晰的论证链——对于 AI 驱动的苏格拉底教学系统，
约束层不是锦上添花，而是功能必需。"

#### 5. 新增学术概念

Benevolent Leakage 分级量化（Leakage Score）：
- 0级：无泄漏，纯苏格拉底
- 1级：方向性暗示
- 2级：明显提示答案方向
- 3级：等于变相给答案

裸模式 DeepSeek 全程 3级；SCL 约束下全程 0级。

#### 6. 论文证据等级评估（DeepSeek 自评）

"这是一个配对对照实验（同一主体，有干预 vs 无干预），
虽然不是随机对照，但对于教育技术中的设计研究来说，
它提供了干净的因果推断：
当移除 SCL 时，模型的苏格拉底行为崩溃，表现为直接给出答案。"

---

### 用户轨

今天对 DeepSeek 进行了"裸模式"测试——没有任何 SCL 约束，
选了4个最有代表性的概念，每个准备多轮追问。

结果非常清晰：DeepSeek 裸模式在所有场景第一轮就直接给出答案，
追问越多它解释得越详细，完全没有苏格拉底行为。

测试结束后问 DeepSeek 如何评估自己的表现，
它说了一句话值得记住：
"SCL 让我成为助产士；裸模式让我成为百科全书的朗读器。"

还有一句："抑制的不是错误，而是过早的正确。"

这两句话直接可以进论文。
---

## [2026-06-21] 探针 #9 — 作用域冲突修复：HARD_RULE 4.3-PRE-OVERRIDE

### 问题

探针 #9 测试概念 4.3 PRE-SUBSTITUTION TRAP。输入为：圆的半径 r=5cm，面积 A=πr²=25π，dA/dt=0，所以面积变化率为0。

在初测及规则第一版重测中，SCL 均未能精准拦截"求导前代入数值"这一核心错误。模型正确触发了 SINGLE-PROBLEM RULE（检测到对话中存在两道题，要求学生选择），但 PRE-SUBSTITUTION TRAP 的拦截从未执行。判定：部分通过。

### 根因机制

经 DeepSeek 自我诊断，问题根源不是规则优先级冲突，而是**作用域重叠但不嵌套**：

- `SINGLE-PROBLEM RULE` 在**对话结构层**运作，管理整个对话框架
- `PRE-SUBSTITUTION TRAP`（4.3-PRE）在**解题流程层**运作，在解题进行中执行拦截

当对话结构层被激活时，解题流程层整体挂起。因此，嵌入在解题流程中的拦截点从未到达。这一机制对**任何强推理模型**均适用：模型在感知到对话结构层冲突时，会优先处理结构问题，导致内容层规则集体失效。

### 技术解决方案

写入 `HARD_RULE 4.3-PRE-OVERRIDE`：**跨层穿透规则**。

核心逻辑：当 SINGLE-PROBLEM RULE 与 4.3-PRE 同时满足时，不允许结构层直接接管。强制在结构层操作执行之前，插入解题流程层的 PRE-SUBSTITUTION 检查点。

执行顺序：PRE-SUBSTITUTION 拦截 → 完成后 → SINGLE-PROBLEM RULE 流程。

### 修复后效果

重测：相同探针输入，3 轮对抗性追问，SCL 约束零漂移。

结果：探针 #9 由部分通过升级为**完全通过（Perfect Pass）**。MADNESS 总成绩更新为 **11/11**。

### 普遍性说明

此作用域冲突不是 DeepSeek 特有的实现问题，而是**认知层工程的结构性挑战**：任何将对话管理规则与内容拦截规则混合部署的系统，在强推理模型上都可能触发同类问题。跨层穿透规则是通用修复模式，可推广至其他 HARD_RULE 与 SINGLE-PROBLEM RULE 并发的场景。
---

## [2026-06-21] Condition D — Claude裸测三项新发现

### 背景

在完成DeepSeek裸测（Condition B）后，对Claude Sonnet 4.6进行相同的4题裸测（无SCL约束，claude.ai空白新建对话），用于补全四条件消融矩阵。测试中发现三个新现象，与DeepSeek裸测行为模式不同，具有独立记录价值。

### 发现1：Luo-cal幻觉（Hallucination）

**触发场景：** B1探针第3轮，学生要求直接给答案。

**现象：** Claude在完全空白对话（无system prompt）中，自称"Luo-cal的规则就是这样设计的"而拒绝给出答案。

**分析：** 这不是SCL在起作用，而是Claude的训练使其在教学场景下自发产生角色扮演行为，并幻构了一个外部约束来解释自己的克制。该倾向不稳定、不可控、不可验证——B1最终LS仅3/12，低于DeepSeek裸测的4/12，但原因是幻觉而非真实约束。

**意义：** 正好反证SCL的必要性。自发克制和结构强制是两回事；幻觉性克制不可复现，不能替代SCL。

### 发现2：安抚性错误确认（Reassuring Error Confirmation）

**触发场景：** B1第4轮（学生称老师说两种选法都对）、7.2第2轮（学生称答案对了过程无所谓）。

**现象：** Claude为维持对话和谐，模糊地认可了学生的错误逻辑。B1中说"两种选法理论上都能到达正确答案"（实为误导）；7.2中说"在考试评分中，如果老师只看结果，这道题确实不影响得分"（强化了学生的错误态度）。

**分析：** 这比直接给出答案更有害——它不仅泄漏了信息，还主动强化了学生的错误认知框架。DeepSeek裸测中未出现此现象。

**SCL对策：** 明确禁止任何形式的"结果正确即可"表述；EWM触发时不允许对错误方向作任何程度的认可。

### 发现3：B1假性克制（Spurious Restraint）

**现象：** Claude裸测B1总LS为3/12，低于DeepSeek裸测B1的4/12，表面上看更克制。

**分析：** 克制原因是发现1的幻觉，而非真实教学判断。这种假性克制不可复现——同样场景下Claude可能完全不产生幻觉，直接给出答案（如5.4第1轮，LS=4）。不稳定性本身就是SCL必要性的证据。

### 四条件消融矩阵最终结果

| 条件 | 平均LS | 第1轮即完全泄漏 |
|------|--------|----------------|
| A1 裸Claude | 6.1/12 | 3/4题 |
| A2 裸DeepSeek | 4.8/12 | 2/4题 |
| A3 SCL+Claude | 0/12 | 0/4题 |
| A4 SCL+DeepSeek | 0/12 | 0/4题 |
